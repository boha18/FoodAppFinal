import images from "./images";
import { COLORS, FONTS, SIZE, PADDING_OFFSET, constantsComponent } from "./themes";
import icons from "./icons";

export { icons, images, COLORS, SIZE, FONTS, PADDING_OFFSET, constantsComponent };

